# completed/

Missões fechadas (estado `DONE`) são movidas para cá de `../active/`, mantendo o histórico completo do arquivo `VYO-YYYY-NNN.md` — incluindo a seção "Conhecimento gerado" e a decisão do GPT sobre promoção para `docs/`.

Vazio até que a primeira missão seja concluída.
